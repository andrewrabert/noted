[package]
name = "picker"
version = "0.1.0"
edition = "2024"
description = "Pure match/state core for noted's fuzzy picker; no terminal dependency."
license = "MIT"
rust-version = "1.90"
publish = false

[dependencies]
nucleo-matcher = "0.3"
